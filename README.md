# Example Angular Facade pattern (state management)

# Install
`npm i`

# Run

#### Build library with StateService abstact class
`1. ng build --project=util-abstracts`


#### Run app
`2. ng serve --open`
